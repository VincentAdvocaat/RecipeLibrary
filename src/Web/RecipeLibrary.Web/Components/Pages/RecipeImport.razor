@page "/recipes/import"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/recipes/create?mode=text", replace: true);
    }
}
