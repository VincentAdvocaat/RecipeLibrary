{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start-sql",
      "type": "shell",
      "command": "docker compose up -d sql --wait",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
